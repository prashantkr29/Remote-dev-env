# Java Development Environment on Kubernetes

## Overview

This manifest deploys a browser-based Java development environment inside Kubernetes with a custom image that includes Java 21 and JVM debugging tools.
The environment allows developers to access a VS Code–like IDE directly from a web browser and work on Java applications within the Kubernetes cluster.


## Purpose
The purpose of this setup is to provide a persistent and easily accessible Java development environment running inside Kubernetes. It can be used for local development, experimentation, or as a base for building a larger developer platform where environments are provisioned dynamically.

<img width="1760" height="962" alt="image" src="https://github.com/user-attachments/assets/a4023d0c-ee44-419f-aecc-2fd8f67ae158" />
